services:
  app:
    build: .
    container_name: event-radar-demo
    restart: always
    ports:
      - "8000:8000"
    environment:
      - DEMO_MODE=1
    env_file:
      - backend/.env
    volumes:
      - ./data:/app/data